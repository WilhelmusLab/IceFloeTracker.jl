# Configuration for OSCAR cluster at Brown University

executor: slurm
jobs: unlimited
default-resources:
  slurm_partition: "batch"  
  slurm_account: "default"  # OSCAR: condo account
  runtime: "10m"

set-resources:
  init_ift:
    runtime: "30m"
    mem: "32g"
    cpus_per_task: 16
  segment:
    runtime: "30m"
    mem: "32g"
  tracking:
    runtime: "8h"
    mem: "64g"
  


partitions:
  batch:
    max_runtime: 720
    max_nodes: 1
    max_cpus_per_task: 16
