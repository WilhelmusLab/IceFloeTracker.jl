import os
from datetime import timedelta, date
import calendar
import pyproj
import pandas as pd

configfile: "workflow/config.yaml"
workdir: "workflow/results"

regions = pd.read_csv(config["REGION_CSV"]).set_index("region")
regions_cases = pd.read_csv(config["REGION_CASE_CSV"])

def daterange(start_date, end_date):
    """Generates dates from start_date to end_date (inclusive)."""
    date = start_date
    while date <= end_date:
        yield date
        date += timedelta(days=1)


def last_day_of_month(year, month):
    """Returns the last day of a given month and year."""
    _, last_day = calendar.monthrange(year, month)
    date_ = date(year, month, last_day)
    return date_


def get_bbox(center_crs, centroid, target_crs, target_width_height):
    """Given a centroid coordinate, its CRS, and a target width and height in meters, 
    returns a bounding box in the target CRS."""
    transformer = pyproj.Transformer.from_crs(
        center_crs, target_crs, always_xy=True)
    centroid = transformer.transform(centroid[1], centroid[0])
    center_round = list(map(round, centroid))
    half_width = target_width_height[0] / 2
    half_height = target_width_height[1] / 2
    min_x = center_round[0] - half_width
    max_x = center_round[0] + half_width
    min_y = center_round[1] - half_height
    max_y = center_round[1] + half_height
    return f"{min_x},{min_y},{max_x},{max_y}"


def get_region_data(region):
    data = regions.loc[region].to_dict()
    return data

def get_region_centroid(region):
    data = get_region_data(region)
    crs, lat, lon = data["center_crs"], float(data["center_lat"]), float(data["center_lon"])
    return crs, (lat, lon)

def get_region_target_extent(region):
    data = get_region_data(region)
    crs, width, height = data["target_crs"], float(data["target_width"]), float(data["target_height"])
    return crs, (width, height)

def get_region_target_bbox(region):
    data = get_region_data(region)
    centroid = get_region_centroid(region)
    extent = get_region_target_extent(region)
    bbox = get_bbox(*centroid, *extent)
    return bbox


# ============================================================================
# Batch Targets
# ============================================================================

rule region_case_results:
    input:
        expand("{case.region}.{case.scale}m.{case.start}.{case.end}.{case.segmentation}.tracked.csv",
            case=regions_cases.itertuples(),
        )
    output:
        "region-case-results.txt"
    shell:        
        """
        for case in {input}; do
            wc "$case" >> {output};
        done

        """

# ============================================================================
# INIT RULES
# ============================================================================


rule init:
    input:
        "init-soit.txt",
        "init-fsdproc.txt",
        "init-colorize.txt",
        "init-csvjoin.txt",
        "init-loader.txt",
        "init-IFTP.txt",

rule init_soit:
    output:
        "init-soit.txt"
    shell:
        "{config[PASS_TIME]} --help > {output}"

rule init_fsdproc:
    output:
        "init-fsdproc.txt"
    shell:
        "{config[FSDPROC]} --help > {output}"

rule init_colorize:
    output:
        "init-colorize.txt"
    shell:
        "{config[COLORIZE]} --help > {output}"

rule init_csvjoin:
    output:
        "init-csvjoin.txt"
    shell:
        "{config[CSVJOIN]} --help > {output}"


rule init_loader:
    output:
        "init-loader.txt"
    shell:
        "{config[LOADER]} --help > {output}"


rule init_iftp:
    output:
        "init-IFTP.txt"
    shell:
        "{config[IFT]} -e '' > {output}"


# ============================================================================
# OVERPASS TIME RULES
# ============================================================================

rule get_region_overpass_times:
    output:
        "{region}/{year}.overpasses.csv"
    wildcard_constraints:
        year = "[0-9]{4}"
    params:
        start = "{year}-01-01",
        end = "{year}-12-31",
        center_lat = lambda wc: get_region_data(wc.region)["center_lat"],
        center_lon = lambda wc: get_region_data(wc.region)["center_lon"]
    shell:
        "{config[PASS_TIME]} --csvoutpath {output} "
        "--startdate {params.start} --enddate {params.end} "
        "--centroid-lat {params.center_lat} --centroid-lon {params.center_lon} "

rule get_region_month_overpass_times:
    output:
        "{region}/{year}.{month}.overpasses.csv"
    params:
        start = "{year}-{month}-01",
        end = lambda wc: last_day_of_month(
            int(wc.year), int(wc.month)).strftime("%Y-%m-%d"),
        center_lat = lambda wc: get_region_data(wc.region)["center_lat"],
        center_lon = lambda wc: get_region_data(wc.region)["center_lon"]
    shell:
        "{config[PASS_TIME]} --csvoutpath {output} "
        "--startdate {params.start} --enddate {params.end} "
        "--centroid-lat {params.center_lat} --centroid-lon {params.center_lon} "


rule get_single_overpass_time:
    output:
        "{region}.{scale}m.{year}-{month}-{day}.{satellite}/overpass.txt"
    input:
        "{region}/{year}.{month}.overpasses.csv"
    wildcard_constraints:
        year = "[0-9]{4}",
        month = "[0-9]{2}",
        day = "[0-9]{2}"
    params:
        date = lambda wc: f"{wc.year}-{wc.month}-{wc.day}"
    run:
        import csv
        overpass_time = None
        with open(input[0]) as f:
            csvfile = csv.reader(f)
            for row in csvfile:
                if row and row[0] == params.date and row[1] == wildcards.satellite:
                    overpass_time = row[2]
                    break
        with open(output[0], "w") as f:
            f.write(overpass_time + "\n")

rule get_case_overpass_time:
    output:
        "{region}.{scale}m.{year}-{month}-{day}.{satellite}/overpass.txt"
    input:
        "{region}.{scale}m.{year}-{month}-{day}.{satellite}/overpass.txt"
    shell:
        "cp {input} {output}"

# ============================================================================
# IMAGE LOADING RULES
# ============================================================================

rule load_truecolor:
    output:
        "{region}.{scale}m.{date}.{satellite}/truecolor.tiff"
    params:
        bbox = lambda wc: get_region_target_bbox(wc.region),
        crs = lambda wc: get_region_data(wc.region)["target_crs"]
    shell:
        "{config[LOADER]} {output} --kind truecolor "
        "--crs {params.crs} --bbox {params.bbox} --scale {wildcards.scale} "
        "--datetime {wildcards.date} --satellite {wildcards.satellite}"

rule load_falsecolor:
    output:
        "{region}.{scale}m.{date}.{satellite}/falsecolor.tiff"
    params:
        bbox = lambda wc: get_region_target_bbox(wc.region),
        crs = lambda wc: get_region_data(wc.region)["target_crs"]
    shell:
        "{config[LOADER]} {output} --kind bands721 "
        "--crs {params.crs} --bbox {params.bbox} --scale {wildcards.scale} "
        "--datetime {wildcards.date} --satellite {wildcards.satellite}"

rule load_cloud:
    output:
        "{region}.{scale}m.{date}.{satellite}/cloud.tiff"
    params:
        bbox = lambda wc: get_region_target_bbox(wc.region),
        crs = lambda wc: get_region_data(wc.region)["target_crs"]
    shell:
        "{config[LOADER]} {output} --kind cloud "
        "--crs {params.crs} --bbox {params.bbox} --scale {wildcards.scale} "
        "--datetime {wildcards.date} --satellite {wildcards.satellite}"


rule load_raw_landmask:
    output:
        "{region}/{scale}m.{year}.landmask_raw.tiff"
    params:
        bbox = lambda wc: get_region_target_bbox(wc.region),
        crs = lambda wc: get_region_data(wc.region)["target_crs"]
    shell:
        "{config[LOADER]} {output} --kind landmask "
        "--crs {params.crs} --bbox {params.bbox} --scale {wildcards.scale} "
        "--datetime {wildcards.year}-01-01"


rule load_landmask:
    output:
        "{stem}landmask.tiff"
    input:
        "{stem}landmask_raw.tiff"
    shell:
        """
        {config[IFT]} -e 'using IceFloeTracker, Images; 
            load("{input}") |> to_landmask .|> Bool .|> Gray |> save("{output}")
        '
        """

rule load_coastal_buffer_mask:
    output:
        "{region}/{scale}m.{year}.coastal_buffer_mask.tiff"
    input:
        landmask = "{region}/{scale}m.{year}.landmask.tiff"
    shell:
        """
        {config[IFT]} -e 'using IceFloeTracker, Images; 
            load("{input.landmask}") |> to_landmask |> create_coastal_buffer .|> Gray |> save("{output}")
        '
        """


rule load_case_landmask:
    output:
        "{region}.{scale}m.{year}-{month}-{day}.{satellite}/landmask.tiff"
    input:
        "{region}/{scale}m.{year}.landmask.tiff"
    shell:
        "cp {input} {output}"


rule load_case_coastal_buffer_mask:
    output:
        "{region}.{scale}m.{year}-{month}-{day}.{satellite}/coastal_buffer_mask.tiff"
    input:
        "{region}/{scale}m.{year}.coastal_buffer_mask.tiff"
    shell:
        "cp {input} {output}"


# ============================================================================
# SEGMENTATION RULES
# ============================================================================
# TODO: add intermediate outputs
# In all cases, add intermediate outputs for:
# - Cloud mask
# - Ice-water discrimination
# - preprocessed image masked with cloud and land-mask
# - preprocessed image
# - K-means result


rule segment:
    output:
        labels_map = "{dir}/{segmentation}.labels_map.tiff",
        cloud_mask = "{dir}/{segmentation}/cloud_mask.tiff",
        ice_mask = "{dir}/{segmentation}/ice_mask.tiff",
        segment_mean_truecolor = "{dir}/{segmentation}/segment_mean_truecolor.tiff",
        segment_mean_falsecolor = "{dir}/{segmentation}/segment_mean_falsecolor.tiff",
    input:
        truecolor = "{dir}/truecolor.tiff",
        falsecolor = "{dir}/falsecolor.tiff",
        landmask = "{dir}/landmask.tiff",
        coastal_buffer_mask = "{dir}/coastal_buffer_mask.tiff",
    shell:
        """
        {config[IFT]} -e 'using IceFloeTracker, Images; {wildcards.segmentation}.Segment()(
            load("{input.truecolor}"),
            load("{input.falsecolor}"),
            load("{input.landmask}") |> to_landmask,
            load("{input.coastal_buffer_mask}") |> to_landmask;
            intermediate_results_callback=call_kwargs(;
                labels_map=l -> l .|> UInt16 |> save("{output.labels_map}"),
                cloud_mask=save("{output.cloud_mask}"),
                ice_mask=save("{output.ice_mask}"),
                segment_mean_truecolor=save("{output.segment_mean_truecolor}"),
                segment_mean_falsecolor=save("{output.segment_mean_falsecolor}"),
            ),
        )'
        """
        

rule segment_buckley:
    output:
        labels_map = "{dir}/buckley.labels_map.tiff",
        props = "{dir}/buckley/props.csv",
        truecolor_cloudmasked = "{dir}/buckley/cloud_mask_on_rgb.tif",
        truecolor_land_cloudmasked = "{dir}/buckley/land_cloud_mask_on_rgb.tif",
    input:
        truecolor = "{dir}/truecolor.tiff",
        cloud_map = "{dir}/cloud.tiff",
        landmask = "{dir}/landmask.tiff",
    shell:
        """
        mkdir -p {wildcards.dir}/buckley;

        {config[FSDPROC]} process \
            {input.truecolor} \
            {input.cloud_map} \
            {input.landmask} \
            {wildcards.dir}/buckley/

        cp {wildcards.dir}/buckley/final.tif {output.labels_map}
        """

ruleorder: segment_buckley > segment

# ============================================================================
# TRACKING RULES
# ============================================================================


rule tracking:
    output:
        tracked = "{region}.{scale}m.{start}.{end}.{segmentation}.tracked.csv"
    input:
        labels_maps = lambda wc: expand("{region}.{scale}m.{date}.{satellite}/{segmentation}.labels_map.tiff",
                          satellite=config["SATELLITES"],
                          date=daterange(date.fromisoformat(wc.start),
                                         date.fromisoformat(wc.end)),
                          **wc
                          ),

        overpasses = lambda wc: expand("{region}.{scale}m.{date}.{satellite}/overpass.txt",
                          satellite=config["SATELLITES"],
                          date=daterange(date.fromisoformat(wc.start),
                                         date.fromisoformat(wc.end)),
                          **wc
                          )
    shell:
        """
        {config[IFT]} -e 'using IceFloeTracker, Images, TimeZones, CSVFiles, DataFrames;
        # Load input data
        overpasses = "{input.overpasses}" |> split .|> (n-> read(n, String)) .|> chomp .|> ZonedDateTime .|> DateTime;
        labels_maps = "{input.labels_maps}" |> split .|> load .|> x -> reinterpret(Int16, x);
        imgs = labels_maps .|> x -> Int16.(x) .|> .>(0) .|> Gray;  # for tracking, we only need the shape of the segments, not the actual labels, so convert to binary masks
        segmented_imgs = map((tc, lm) -> SegmentedImage(tc, Int64.(lm)), imgs, labels_maps);

        # Sort into chronological order
        order = sortperm(overpasses);
        overpasses = overpasses[order];
        segmented_imgs = segmented_imgs[order];

        tracker = FloeTracker(
            filter_function=FilterFunction(),
            matching_function=MinimumWeightMatchingFunction()
        );
        tracked = tracker(segmented_imgs, overpasses);
        select!(tracked, Not(:mask, :psi));
        save("{output.tracked}", tracked);
        '
        """


# ============================================================================
# UTILITY RULES
# ============================================================================


rule colorize_labeled:
    input:
        labeled = "{stem}labels_map.tiff",
        init = "init-colorize.txt"
    output:
        colorized = "{stem}labels_map.colorized.tiff"
    shell:
        "{config[COLORIZE]} {input.labeled} {output.colorized}"
